import HeroLanding from "@/components/HeroLanding";

// The hero IS the app: the mic, the conversation, and the results all live on this
// one page (HeroLanding is a client component that handles the voice flow inline).
export default function Home() {
  return <HeroLanding />;
}
